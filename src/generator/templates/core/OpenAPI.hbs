/* istanbul ignore file */
/* tslint:disable */
/* eslint-disable */
/* prettier-ignore */

interface Config {
    SAML_URL: string;
    BASE: string;
    VERSION: string;
    TOKEN: string;
    SECURITY: { [definedName: string]: object };
}

export const OpenAPI: Config = {
    SAML_URL: '{{{samlUrl}}}',
    BASE: '{{{server}}}',
    VERSION: '{{{version}}}',
    TOKEN: '',
    SECURITY: {
    {{#each security}}
        '{{@key}}': {
            {{#each this}}
            '{{@key}}': '{{this}}',
            {{/each}}
        },
    {{/each}}
    }
};